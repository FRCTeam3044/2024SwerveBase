[
  {
    "type": "while",
    "name": "Drive Until Note",
    "condition": {
      "id": "not",
      "children": [
        {
          "id": "note_in_area",
          "parameters": {
            "regionX": "{{ regionX }}",
            "regionY": "{{ regionY }}",
            "regionRadius": 1
          },
          "children": []
        }
      ],
      "parameters": {}
    },
    "child": {
      "type": "command",
      "id": "go_to_and_track_point",
      "parameters": {
        "targetX": "{{ regionX }}",
        "targetY": "{{ regionY }}",
        "trackX": "{{ regionX }}",
        "trackY": "{{ regionY }}"
      },
      "name": "Go To Top Note"
    }
  },
  {
    "type": "if",
    "name": "Note In Area",
    "condition": {
      "id": "note_in_area",
      "parameters": {
        "regionX": "{{ regionX }}",
        "regionY": "{{ regionY }}",
        "regionRadius": "{{ regionRadius }}"
      },
      "children": []
    }
  },
  {
    "type": "command",
    "id": "go_to_note_in_area",
    "name": "Go To Note",
    "parameters": {
      "regionX": "{{ regionX }}",
      "regionY": "{{ regionY }}",
      "regionRadius": "{{ regionRadius }}"
    }
  }
]
